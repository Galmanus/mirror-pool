[package]
name = "riverrun-core"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Hash-based, post-quantum anonymity-set primitives for mirror-pool: commitments, Merkle set, nullifiers, and membership proofs."

[dependencies]
blake3.workspace = true
thiserror.workspace = true
# Direct OS CSPRNG for minting member secrets. A privacy tool that leaves secret
# generation to the caller ships a footgun; `random()` closes that.
getrandom = "0.2"

[dev-dependencies]
