[package]
name = "mirror-eval"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Adversarial evaluation harness for mirror-pool: runs real chain-clustering attacks against a simulated pool and measures attribution success."

[dependencies]
mirror-core = { path = "../mirror-core" }

[[bin]]
name = "mirror-eval"
path = "src/main.rs"
